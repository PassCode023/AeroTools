# 微信开发者工具的个人配置(每人不同,不入库)
project.private.config.json

# ZCode 本地会话文件
.zcode/

# git worktree 本地工作区
.worktrees/

# 机场数据上游原始文件（仅本地保留，登记 SHA-256，不入库）
airport-data/raw/

# 构建环境依赖（package.json/package-lock.json 入库，包体不入库）
airport-data/node_modules/

# AI 会话本地工作目录（不入库）
app/.ai/
